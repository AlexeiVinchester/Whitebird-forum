import { TRoutes } from "../types/routes.type";

export const ROUTES: TRoutes = {
    HOME: "/",
    POSTS: "/posts",
    POST_DETAIL: "/posts/:postId",
    PROFILE: "/profile",
    LOGIN: "/login",
    USERS: "/users",
    NOT_FOUND: "*",
};